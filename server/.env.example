# Copy to .env (or export these) to enable real Claude responses.
# Without it, the server returns clearly-labelled mock answers so the app still runs.
ANTHROPIC_API_KEY=

# Optional — port the API listens on (default 8787).
PORT=8787
