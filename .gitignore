node_modules/
dist/
.env
server/comparisons.json
*.log
.DS_Store

# runtime stores (match behavior etc. written under lms/)
server/lms/match_*.json

# runtime json stores written under server/lms/ (corpus, behavior, learning, admin, wallet)
server/lms/*.json
